// TODO: write your code here
import ArrayBufferConverter from './ArrayBufferConverter.js';

// eslint-disable-next-line no-unused-vars
const p1 = new ArrayBufferConverter();
p1.load();
p1.toString();
console.log(p1, p1.load(), p1.toString());
